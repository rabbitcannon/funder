<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Player Required Fields
    |
    | These are the fields that MUST come in on every SPAT (X-Auth-SPAT) header in order
    | to accept the player as valid. The registrar_id is always required, so no need
    | to specify that. email and player_state are common fields you might require
    |
    */

    "required" => [
    ]

];
